// Stub for TaskForm component
import React from 'react';
export default function TaskForm({ onSaveSuccess, projectId, isManager }: { onSaveSuccess: () => void; projectId: string; isManager: boolean }) {
  return <div>Task Form (stub)</div>;
}
