import Navbar from "../components/common/Navbar";

export default function ManagerDashboard() {
//   const { token } = useAuth();
  // TODO: Fetch projects, team members, tasks, etc.
  return (
    <div>
        <Navbar/>
      <h1 className="text-2xl font-bold mb-4">Manager Dashboard</h1>

    </div>
  );
}
